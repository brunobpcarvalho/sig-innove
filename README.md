# SIG INNOVE

docker build -t sig-innove .

#### 1. Instalar dependências da aplicação:

```console
$ npm install
```

#### 3. Iniciar containers do Docker:

```console
$ docker-compose up
```
sudo docker-compose up

&& cd sig-innove